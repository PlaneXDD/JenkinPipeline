pipeline {
    agent any

    environment {
        EMAIL_CREDS = credentials('gmail-creds')  // Make sure this matches your Jenkins credential ID
    }

    stages {
        stage('Prepare Environment') {
            steps {
                echo 'Preparing the environment...'
            }
        }

        stage('Build') {
            steps {
                echo 'Building the project...'
                bat 'echo Build successful' // For Windows, or sh for Linux/MacOS
            }
        }

        stage('Test') {
            steps {
                echo 'Running tests...'
                bat 'echo Tests passed' // For Windows, or sh for Linux/MacOS
            }
        }

        stage('Package') {
            steps {
                echo 'Packaging the application...'
                bat 'echo Packaging done' // For Windows, or sh for Linux/MacOS
            }
        }
    }

    post {
        always {
            // Archive the console log as an artifact
            archiveArtifacts artifacts: 'console.log', allowEmptyArchive: true
            // Save the build log to a file
            script {
                writeFile file: 'console.log', text: currentBuild.rawBuild.getLog()
            }
        }

        success {
            // Send success notification email with logs attached
            emailext (
                to: 'phamthienphu2604@gmail.com',
                subject: "Pipeline succeeded: ${currentBuild.fullDisplayName}",
                body: "The pipeline has completed successfully. Please find the logs attached.",
                attachmentsPattern: 'console.log',
                mimeType: 'text/plain',
                from: "${env.EMAIL_CREDS_USR}"
            )
        }
        failure {
            // Send failure notification email with logs attached
            emailext (
                to: 'phamthienphu2604@gmail.com',
                subject: "Pipeline failed: ${currentBuild.fullDisplayName}",
                body: "The pipeline has failed. Please find the logs attached.",
                attachmentsPattern: 'console.log',
                mimeType: 'text/plain',
                from: "${env.EMAIL_CREDS_USR}"
            )
        }
    }
}
